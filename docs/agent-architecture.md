# Agent Architecture

Currently there is a single agent within kwaak, eventually kwaak will support
multiple agents and agent teams. 

The entry point for learning about the agent is `src/agent/v1.rs`

The kwaak agent is structured as follows:

the agent builder is the loop that the agent runs. 
Docs on agents are:
https://swiftide.rs/agents/overview/
https://docs.rs/swiftide/latest/swiftide/agents/index.html


```


```
